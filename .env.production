# Production build (next build / pnpm build) — embeds this URL into the static bundle.
# Must be HTTPS so the deployed (HTTPS) frontend can call it without mixed-content errors.
NEXT_PUBLIC_API_URL=https://gobia-hackathom-production.up.railway.app
